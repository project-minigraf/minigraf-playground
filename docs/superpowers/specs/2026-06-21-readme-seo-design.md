---
title: README & SEO Metadata
date: 2026-06-21
status: approved
---

# README & SEO Metadata

## Problem

The deployed playground returns `<title>Create Next App</title>` and the meta description reads "Generated by create next app". Every link preview (Twitter, Slack, HN) and every search/LLM-ingestion hit shows the wrong title. The README is also unmodified create-next-app boilerplate.

Additionally, the Minigraf GitHub URL across the codebase references `adityamukho/minigraf` (stale personal fork) instead of the canonical `project-minigraf/minigraf`.

## Scope

Three changes:

1. **Replace README** with a user-facing project page.
2. **Fix `app/layout.tsx` metadata** — proper title, description, OG tags, canonical URL.
3. **Add `app/opengraph-image.tsx`** — dynamic OG image via Next.js `ImageResponse`.
4. **Update stale `adityamukho/minigraf` refs** in `AGENTS.md`, `docs/superpowers/specs/2026-04-20-minigraf-playground-design.md`, and `docs/superpowers/plans/2026-04-20-minigraf-playground.md`.

---

## README

Target audience: users who want to explore the playground. No setup instructions.

```markdown
# Minigraf Playground

An interactive, browser-based tutorial for [Minigraf](https://github.com/project-minigraf/minigraf) —
a tiny embedded graph database with Datalog querying and bi-temporal time travel.

**[Open the Playground →](https://minigraf-playground.vercel.app)**

## What's inside

Five tutorials, each building toward progressively more advanced Datalog:

- **Basic Datalog** — facts, rules, and simple graph queries
- **Marketplace** — multi-hop paths and recursive reachability
- **Org Chart** — hierarchies, recursive ancestry, and negation
- **Sports League** — bi-temporal contracts, transfer chains, and aggregates
- **City Transit** — timetable queries, reachability under constraints, and negation

Plus a **Sandbox** mode — write your own facts and rules against a persistent local graph,
with the AI tutor available as you explore.

Each tutorial has an AI tutor (powered by your own API key or a free Groq fallback)
that can answer questions and explain your results.

## Privacy

Your API key never leaves your browser. All graph state and conversation history
are stored locally in IndexedDB. Nothing is sent to this app's servers.
```

---

## `app/layout.tsx` — metadata export

```ts
export const metadata: Metadata = {
  metadataBase: new URL(process.env.NEXT_PUBLIC_APP_URL!),
  title: "Minigraf Playground",
  description:
    "Interactive browser-based tutorials for Minigraf, a graph database with Datalog and bi-temporal time travel",
  openGraph: {
    title: "Minigraf Playground",
    description:
      "Interactive browser-based tutorials for Minigraf, a graph database with Datalog and bi-temporal time travel",
    url: process.env.NEXT_PUBLIC_APP_URL,
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Minigraf Playground",
    description:
      "Interactive browser-based tutorials for Minigraf, a graph database with Datalog and bi-temporal time travel",
  },
};
```

`metadataBase` makes the OG image URL resolve correctly. Next.js automatically picks up `app/opengraph-image.tsx` and appends it to the OG image list.

---

## `app/opengraph-image.tsx` — dynamic OG image

- Dimensions: 1200×630 (standard)
- Background: `#0a0a0a` (matches app dark theme)
- Headline: "Minigraf Playground" — large white text, top-left aligned
- Subtitle: the 110-char description — muted gray (`#a1a1aa`), below headline
- Footer: `minigraf-playground.vercel.app` — smaller, more muted (`#52525b`), bottom-right

Uses `ImageResponse` from `next/og`. No external fonts or images — system font stack only, so generation is fast and cold-starts are not a concern.

Exported `size` and `contentType` as required by Next.js convention.

---

## Stale ref updates

Replace `adityamukho/minigraf` → `project-minigraf/minigraf` in:

- `AGENTS.md` (line 9)
- `docs/superpowers/specs/2026-04-20-minigraf-playground-design.md` (line 10)
- `docs/superpowers/plans/2026-04-20-minigraf-playground.md` (line 267, inside a JSON snippet)

The `.worktrees/` copies are excluded — isolated worktrees track their own state.

---

## Files changed

| File | Action |
|------|--------|
| `README.md` | Rewrite |
| `app/layout.tsx` | Update `metadata` export |
| `app/opengraph-image.tsx` | Create |
| `AGENTS.md` | Fix stale URL |
| `docs/superpowers/specs/2026-04-20-minigraf-playground-design.md` | Fix stale URL |
| `docs/superpowers/plans/2026-04-20-minigraf-playground.md` | Fix stale URL |

---

## Definition of done

- [ ] `<title>` in deployed HTML reads "Minigraf Playground"
- [ ] `<meta name="description">` reads the 110-char description
- [ ] OG image renders at `/opengraph-image` (verify with Next.js dev tools or a link preview checker)
- [ ] All `adityamukho/minigraf` refs replaced in in-tree docs
- [ ] `npm run build` passes
- [ ] Single commit: `docs: update README and add SEO metadata`
